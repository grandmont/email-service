syntax = "proto3";

package mailPackage;

service Mail {
    rpc sendMail(MailRequest) returns(Response);
}

message Void {}

message MailRequest {
    string from = 1;
    string to = 2;
    string subject = 3;
    string html = 4;
}

message Response {
    bool status = 1;
}
